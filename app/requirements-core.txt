# Только для Core API в Docker (main.py, api, stage1–stage3, stage5).
# Без TTS/torch — озвучка в отдельном сервисе.

fastapi
uvicorn[standard]
pydantic
python-multipart
requests
boto3
redis>=5.0.0

numpy<2.0
scipy
soundfile

razdel>=0.5.0
natasha>=1.5.0
regex>=2023.0.0
pydub>=0.25.1
tqdm>=4.66.0
