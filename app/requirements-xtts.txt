# XTTS2 (Coqui) TTS Engine — устанавливать в отдельный venv (Python 3.10)
# torch 2.2.2 + torchaudio 2.2.2 избегают torchcodec (FFmpeg DLL) на Windows
torch==2.2.2
torchaudio==2.2.2
# Coqui TTS
TTS==0.22.0
# Сервис
fastapi
uvicorn[standard]
pyyaml
pydantic
soundfile
